# Memory debug

Off by default. Set `[Debug] MemoryDebug = 1`, then press **Ctrl+F1**.

Raw readouts and the search tools the rest of the mod was built with: entity list, hitbox dump, move
state, a global scanner, a diff search, a pointer follower and a struct viewer.
